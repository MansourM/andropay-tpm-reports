# راهنمای شروع سریع

این راهنما به شما کمک می‌کند در کمتر از 5 دقیقه اولین گزارش خود را تولید کنید.

## ✅ پیش‌نیازها

قبل از شروع، مطمئن شوید که موارد زیر نصب شده‌اند:

- [ ] Python 3.8 یا بالاتر
- [ ] GitHub CLI (gh)
- [ ] دسترسی به پروژه GitHub

---

## 🚀 نصب در 3 مرحله

### مرحله 1: نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### مرحله 2: احراز هویت GitHub

```bash
gh auth login
gh auth refresh -s project
```

### مرحله 3: تنظیمات

```bash
# کپی فایل نمونه
cp config.example.json config.json

# ویرایش تنظیمات (با ویرایشگر دلخواه)
notepad config.json  # Windows
nano config.json     # Linux/Mac
```

**محتوای config.json:**
```json
{
  "owner": "TechBurst-Pro",
  "project_number": 2,
  "default_format": "html",
  "output_directory": "reports"
}
```

---

## 📊 اولین گزارش

### دستور ساده

```bash
python -m src.main
```

**خروجی:**
```
📋 Loading configuration...
🔍 Fetching project data from GitHub...
   Owner: TechBurst-Pro
   Project: #2
   Found 76 items
⚙️  Processing data...
   Total items: 76
   Completion: 26.3%
   Unplanned: 18.4%
💾 Saving snapshot...
   Saved to: snapshots/snapshot-20251108-154530.json
📝 Generating HTML report...
✅ Report generated successfully!
   Output: reports/report.html
```

### مشاهده گزارش

گزارش در `reports/report.html` ذخیره شده است. فایل را با مرورگر باز کنید:

**Windows:**
```bash
start reports/report.html
```

**macOS:**
```bash
open reports/report.html
```

**Linux:**
```bash
xdg-open reports/report.html
```

---

## 🎯 دستورات پرکاربرد

### فرمت‌های مختلف

```bash
# HTML (با نمودارهای تعاملی)
python -m src.main --format html

# Markdown (برای مستندسازی)
python -m src.main --format md

# CSV (برای Excel)
python -m src.main --format csv

# JSON (برای پردازش خودکار)
python -m src.main --format json
```

### مسیر سفارشی

```bash
# ذخیره در مسیر دلخواه
python -m src.main --output my-reports/weekly-report.html
```

### پروژه دیگر

```bash
# تولید گزارش برای پروژه دیگر
python -m src.main --owner MyOrg --project 5
```

---

## 📖 محتوای گزارش HTML

گزارش HTML شامل بخش‌های زیر است:

### 1. خلاصه آمار
- تعداد کل آیتم‌ها
- تخمین کل ساعت
- درصد تکمیل (با رنگ‌بندی)
- درصد برنامه‌ریزی نشده
- آیتم‌های با اولویت بالا که شروع نشده‌اند

### 2. نمودارهای تعاملی
- **توزیع وضعیت:** نمودار دایره‌ای و میله‌ای
- **توزیع اولویت:** نمودار میله‌ای افقی با رنگ‌بندی
- **برنامه‌ریزی شده/نشده:** نمودار دایره‌ای
- **بار کاری تیم:** نمودار میله‌ای با هشدار اضافه‌بار

### 3. آیتم‌های با اولویت بالا
لیست تمام آیتم‌های P🔥 و P0 با:
- عنوان و لینک
- وضعیت فعلی
- مسئولین
- تخمین ساعت

### 4. آیتم‌ها بر اساس وضعیت
گروه‌بندی آیتم‌ها بر اساس:
- Backlog
- Todo
- Pending
- In Progress
- In Review
- Done

### 5. جدول کامل
جدول قابل مرتب‌سازی با تمام اطلاعات آیتم‌ها

---

## 🎨 رنگ‌بندی معیارها

### درصد تکمیل
- 🟢 **سبز (>70%):** پیشرفت خوب
- 🟡 **زرد (30-70%):** پیشرفت متوسط
- 🔴 **قرمز (<30%):** نیاز به توجه

### برنامه‌ریزی نشده
- 🟢 **سبز (<10%):** برنامه‌ریزی خوب
- 🟡 **زرد (10-20%):** قابل قبول
- 🔴 **قرمز (>20%):** مشکل در برنامه‌ریزی

### بار کاری تیم
- 🟢 **سبز (<6 آیتم):** بار کاری مناسب
- 🟡 **زرد (6-10 آیتم):** بار کاری متوسط
- 🔴 **قرمز (>10 آیتم):** اضافه‌بار

---

## 🔧 عیب‌یابی سریع

### خطا: GitHub CLI یافت نشد

```bash
# بررسی نصب
gh --version

# نصب (Windows)
winget install --id GitHub.cli

# نصب (macOS)
brew install gh

# نصب (Linux)
sudo apt install gh  # Debian/Ubuntu
```

### خطا: احراز هویت ناموفق

```bash
# ورود مجدد
gh auth login

# تازه‌سازی با دسترسی project
gh auth refresh -s project

# بررسی وضعیت
gh auth status
```

### خطا: پروژه یافت نشد

```bash
# بررسی دسترسی
gh project view 2 --owner TechBurst-Pro

# لیست پروژه‌ها
gh project list --owner TechBurst-Pro
```

### خطا: ماژول یافت نشد

```bash
# نصب مجدد وابستگی‌ها
pip install -r requirements.txt

# یا نصب تک‌تک
pip install jinja2 plotly python-dateutil pytest
```

---

## 📚 مراحل بعدی

حالا که اولین گزارش را تولید کردید:

1. **مطالعه مستندات کامل:**
   - [راهنمای استفاده](./USAGE-GUIDE.md)
   - [راهنمای GitHub CLI](./GITHUB-CLI-REFERENCE.md)

2. **سفارشی‌سازی:**
   - تنظیم فایل config.json
   - انتخاب فرمت مناسب
   - تنظیم مسیر خروجی

3. **اتوماسیون:**
   - ایجاد اسکریپت برای گزارش‌های روزانه
   - تنظیم Task Scheduler (Windows) یا Cron (Linux/Mac)
   - یکپارچه‌سازی با Slack/Teams

4. **تست قابلیت‌ها:**
   - تولید گزارش با فرمت‌های مختلف
   - بررسی نمودارهای تعاملی
   - مقایسه اسنپ‌شات‌های هفتگی

---

## 💡 نکات مفید

### نکته 1: گزارش سریع برای استندآپ

```bash
python -m src.main --format md --output daily-standup.md --no-snapshot
```

### نکته 2: صادرات داده برای تحلیل

```bash
python -m src.main --format csv --output data.csv --no-snapshot
python -m src.main --format json --output data.json --no-snapshot
```

### نکته 3: گزارش چند پروژه

```bash
# پروژه 1
python -m src.main --project 1 --output reports/project-1.html

# پروژه 2
python -m src.main --project 2 --output reports/project-2.html
```

### نکته 4: بدون ذخیره اسنپ‌شات

برای گزارش‌های موقت یا تست:

```bash
python -m src.main --no-snapshot
```

---

## ✨ ویژگی‌های کلیدی

- ✅ **تک‌فایله:** گزارش HTML بدون نیاز به فایل‌های خارجی
- ✅ **آفلاین:** بعد از تولید، نیازی به اینترنت نیست
- ✅ **واکنش‌گرا:** روی موبایل، تبلت و دسکتاپ کار می‌کند
- ✅ **فارسی:** پشتیبانی کامل از RTL و فونت فارسی
- ✅ **تعاملی:** نمودارها قابل کلیک و جدول قابل مرتب‌سازی
- ✅ **رنگی:** رنگ‌بندی هوشمند برای شناسایی سریع مشکلات
- ✅ **ردیابی:** مقایسه خودکار با هفته قبل

---

## 🎉 موفق باشید!

حالا آماده‌اید برای تولید گزارش‌های حرفه‌ای از پروژه‌های GitHub!

**سوال دارید؟**
- مستندات کامل را بخوانید
- بخش عیب‌یابی را بررسی کنید
- یک Issue در GitHub ایجاد کنید

---

**زمان تقریبی راه‌اندازی:** 5 دقیقه  
**سطح مهارت مورد نیاز:** مبتدی  
**آخرین بروزرسانی:** 2025-11-08
