# GitHub Projects HTML Reporter

گزارش‌ساز پروژه‌های گیت‌هاب با خروجی HTML زیبا و تعاملی

## 📋 فهرست مطالب

- [نصب](#نصب)
- [پیش‌نیازها](#پیشنیازها)
- [استفاده](#استفاده)
- [تنظیمات](#تنظیمات)
- [فرمت‌های خروجی](#فرمتهای-خروجی)
- [ویژگی‌ها](#ویژگیها)
- [ساختار پروژه](#ساختار-پروژه)
- [تست](#تست)
- [عیب‌یابی](#عیبیابی)

## نصب

### 1. کلون کردن مخزن

```bash
git clone <repository-url>
cd andropay-reports
```

### 2. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### 3. نصب GitHub CLI

این ابزار برای دریافت داده از GitHub CLI استفاده می‌کند:

**Windows:**
```bash
winget install --id GitHub.cli
```

**macOS:**
```bash
brew install gh
```

**Linux:**
```bash
# Debian/Ubuntu
sudo apt install gh

# Fedora/RHEL
sudo dnf install gh
```

برای اطلاعات بیشتر: https://cli.github.com/

### 4. احراز هویت GitHub

```bash
gh auth login
```

## پیش‌نیازها

- Python 3.8 یا بالاتر
- GitHub CLI (gh)
- دسترسی به GitHub Projects مورد نظر

## استفاده

### استفاده پایه

```bash
# تولید گزارش HTML با تنظیمات پیش‌فرض
python -m src.main
```

### گزینه‌های خط فرمان

```bash
# انتخاب فرمت خروجی
python -m src.main --format html    # HTML (پیش‌فرض)
python -m src.main --format md      # Markdown
python -m src.main --format csv     # CSV
python -m src.main --format json    # JSON

# مشخص کردن مسیر خروجی
python -m src.main --output reports/my-report.html

# مشخص کردن پروژه
python -m src.main --owner MyOrg --project 5

# استفاده از فایل تنظیمات سفارشی
python -m src.main --config my-config.json

# غیرفعال کردن ذخیره اسنپ‌شات
python -m src.main --no-snapshot

# نمایش راهنما
python -m src.main --help
```

### مثال‌های کاربردی

```bash
# تولید گزارش Markdown برای پروژه خاص
python -m src.main --owner TechBurst-Pro --project 2 --format md

# تولید CSV و ذخیره در مسیر سفارشی
python -m src.main --format csv --output exports/project-data.csv

# تولید گزارش بدون ذخیره اسنپ‌شات
python -m src.main --no-snapshot
```

## تنظیمات

فایل `config.json` را در ریشه پروژه ایجاد کنید:

```json
{
  "owner": "TechBurst-Pro",
  "project_number": 2,
  "default_format": "html",
  "output_directory": "reports"
}
```

### توضیح تنظیمات

- **owner**: نام سازمان یا کاربر GitHub
- **project_number**: شماره پروژه
- **default_format**: فرمت پیش‌فرض خروجی (`html`, `md`, `csv`, `json`)
- **output_directory**: پوشه ذخیره گزارش‌ها

**نکته:** آرگومان‌های خط فرمان بر تنظیمات فایل اولویت دارند.

## فرمت‌های خروجی

### HTML (پیش‌فرض)
- گزارش تک‌فایله با تمام CSS و JavaScript جاسازی شده
- نمودارهای تعاملی با Plotly
- طراحی واکنش‌گرا (Responsive)
- پشتیبانی کامل از RTL و فارسی
- جدول قابل مرتب‌سازی

### Markdown
- فرمت متنی ساده
- جداول برای نمایش داده‌ها
- لینک‌های مستقیم به GitHub
- ایموجی برای نمایش وضعیت معیارها

### CSV
- قابل باز شدن در Excel
- کدگذاری UTF-8 با BOM
- تمام فیلدهای آیتم‌ها

### JSON
- ساختار داده کامل
- شامل متادیتا و معیارها
- مناسب برای پردازش خودکار

## ویژگی‌ها

### 📊 معیارهای جامع
- تعداد کل آیتم‌ها و تخمین ساعت
- درصد تکمیل با رنگ‌بندی
- تحلیل کارهای برنامه‌ریزی شده vs برنامه‌ریزی نشده
- آیتم‌های با اولویت بالا که شروع نشده‌اند

### 📈 نمودارهای تعاملی
- توزیع وضعیت (Pie و Bar Chart)
- توزیع اولویت با رنگ‌بندی
- نمودار برنامه‌ریزی شده/نشده
- بار کاری تیم با هشدار اضافه‌بار

### 🎨 رنگ‌بندی هوشمند
- **سبز**: وضعیت خوب (تکمیل >70%, برنامه‌ریزی نشده <10%)
- **زرد**: وضعیت متوسط
- **قرمز**: نیاز به توجه (تکمیل <30%, برنامه‌ریزی نشده >20%)

### 📅 ردیابی هفتگی
- ذخیره خودکار اسنپ‌شات
- مقایسه با هفته قبل
- نمایش آیتم‌های تکمیل شده و اضافه شده

### 🌐 پشتیبانی کامل فارسی
- طراحی RTL
- فونت Vazir
- برچسب‌ها و متن‌های فارسی

## ساختار پروژه

```
github-reporter/
├── src/                        # کد اصلی برنامه
│   ├── main.py                # نقطه ورود CLI
│   ├── models.py              # مدل‌های داده (Enums, Dataclasses)
│   ├── config.py              # مدیریت تنظیمات
│   ├── fetcher.py             # دریافت داده از GitHub CLI
│   ├── processor.py           # پردازش و محاسبه معیارها
│   ├── snapshot.py            # مدیریت اسنپ‌شات‌های هفتگی
│   ├── renderers/             # رندرکننده‌های خروجی
│   │   ├── base.py           # کلاس پایه
│   │   ├── html_renderer.py  # رندرر HTML
│   │   ├── md_renderer.py    # رندرر Markdown
│   │   ├── csv_renderer.py   # رندرر CSV
│   │   └── json_renderer.py  # رندرر JSON
│   ├── charts/                # تولید نمودارها
│   │   └── chart_builder.py  # ساخت نمودارهای Plotly
│   └── templates/             # قالب‌های HTML
│       └── report.html        # قالب Jinja2
├── tests/                      # تست‌های واحد
│   ├── test_models.py
│   ├── test_config.py
│   ├── test_fetcher.py
│   ├── test_processor.py
│   ├── test_snapshot.py
│   ├── test_chart_builder.py
│   └── test_html_renderer.py
├── snapshots/                  # اسنپ‌شات‌های ذخیره شده (ایجاد خودکار)
├── reports/                    # گزارش‌های تولید شده (ایجاد خودکار)
├── config.json                 # فایل تنظیمات
├── requirements.txt            # وابستگی‌های Python
└── README.md                   # این فایل
```

## تست

### اجرای تمام تست‌ها

```bash
pytest
```

### اجرای با نمایش Coverage

```bash
pytest --cov=src tests/
```

### اجرای تست‌های خاص

```bash
# تست یک ماژول
pytest tests/test_processor.py -v

# تست یک کلاس
pytest tests/test_processor.py::TestMetricsCalculation -v

# تست یک تابع
pytest tests/test_processor.py::TestMetricsCalculation::test_should_calculate_completion_percentage -v
```

### نتایج تست

پروژه دارای **121 تست** است که تمام قابلیت‌های اصلی را پوشش می‌دهند.

## عیب‌یابی

### خطا: GitHub CLI یافت نشد

```
❌ GitHub CLI Error: GitHub CLI (gh) is not installed.
```

**راه‌حل:** GitHub CLI را نصب کنید (مراجعه به بخش [نصب](#نصب))

### خطا: احراز هویت ناموفق

```
❌ GitHub CLI Error: authentication required
```

**راه‌حل:**
```bash
gh auth login
```

### خطا: پروژه یافت نشد

```
❌ GitHub CLI Error: project not found
```

**راه‌حل:** بررسی کنید:
- `owner` صحیح است؟
- `project_number` درست است؟
- دسترسی به پروژه دارید؟

### خطا: تنظیمات نامعتبر

```
❌ Configuration Error: project_number must be positive
```

**راه‌حل:** فایل `config.json` یا آرگومان‌های CLI را بررسی کنید.

### مشکل در نمایش فارسی

اگر متن‌های فارسی به درستی نمایش داده نمی‌شوند:
- مطمئن شوید فایل با کدگذاری UTF-8 ذخیره شده
- در مرورگر، از فونت‌های پشتیبانی‌کننده فارسی استفاده کنید

## مستندات تکمیلی

برای اطلاعات بیشتر:
- **Requirements:** `.kiro/specs/github-projects-html-reporter/requirements.md`
- **Design:** `.kiro/specs/github-projects-html-reporter/design.md`
- **Tasks:** `.kiro/specs/github-projects-html-reporter/tasks.md`

## مجوز

این پروژه برای استفاده داخلی TechBurst-Pro توسعه یافته است.

## پشتیبانی

برای گزارش مشکلات یا پیشنهادات، لطفاً یک Issue ایجاد کنید.
